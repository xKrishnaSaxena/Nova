import MintTokenForm from "../../components/tokenERC/ERC20MintTokenForm";

const MintTokenPageERC = () => {
  return (
    <div className="min-h-screen flex items-center justify-center bg-gray-100">
      <MintTokenForm />
    </div>
  );
};

export default MintTokenPageERC;
